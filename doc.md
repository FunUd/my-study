---
name: document-authoring-quality
description: 文書全般を新規作成、再構成、または大幅に修正するときに使用する。仕様書、設計書、調査資料、報告書、手順書などが対象。単純な誤字脱字・一文だけの修正・コードのみの変更・文書種別固有のルールだけを適用する作業には使用しない。
---

# Document Writing Quality

## 使用する場合

以下の作業で使用する。

- 文書の新規作成
- 文書全体の再構成・改善
- 仕様書、設計書、調査資料、報告書、手順書、計画書などの作成
- 複数の章・節にまたがる大幅な修正

## 使用しない場合

以下の場合は原則として使用しない。

- コードのみの変更
- 単純な誤字脱字の修正
- 一文や短い文言だけの変更
- 文書種別固有のルールだけを適用する作業

文書種別専用のSkillがある場合は、本Skillの品質方針と専用Skillを併用する。文書形式、必須項目、記載ルールなどの種別固有要件は専用Skillを優先する。

## 目的

目的と読者に適した情報を、必要十分な量と理解しやすい構成で提供する。

過剰な網羅、不要な重複、分かりにくい構成、既存文書との不整合、不正確な記述、将来の更新を妨げる構成を避ける。

## 原則

### 1. 目的・読者に合わせる

作成前に、以下を確認する。

- 目的
- 対象読者と前提知識
- 用途
- 対象範囲
- 必須要件・制約
- 出力形式

以下の情報が不明で、回答によって構成や記載内容が変わる場合は、執筆前にユーザーへ確認する。

- 対象読者
- 対象範囲
- 必須要件・制約
- 文書の用途

回答が得られない場合は、妥当な前提を明示して進める。前提によって確定できない事項は、`{TODO: 内容}`として未確定事項を明示する。

表現や細かな書式など、後から容易に修正できる事項は妥当な前提を置いて進める。

### 2. 必要十分な情報にする

情報量が多いほど良いとは考えない。

目的達成に必要な情報を記載し、不要な情報・重複・一般論・過剰な補足を削る。

特に以下を機械的に行わない。

- 情報を際限なく網羅する
- テンプレートの全項目を埋める
- すべてを同じ粒度で記述する
- 不要な代替案を列挙する
- 他文書の内容を重複して記載する

対象範囲と対象外範囲も明確にする。

### 3. 理解しやすい順序・粒度にする

本文を書く前に、以下を整理する。

- 全体構成
- 各章・節の役割
- 情報の依存関係
- 読者に必要な前提
- 重要情報と補足情報
- 各項目の粒度

読者が全体像を把握し、必要な情報へ到達しやすい順序にする。

結論、判断、手順、要求事項など、読者が早期に把握する必要がある情報は、可能な限り前方に配置する。ただし、定義・背景・前提を先に示さないと理解できない場合は、それらを先に配置する。

見出し、短い段落、箇条書き、表、図などを使い分ける。見出しだけでも文書の全体像を把握できる構成を目指す。

### 4. 既存ドキュメントと整合させる

関連文書がある場合は、作成前に確認する。

優先して確認するもの：

- README、docs等のプロジェクト文書
- 同種の既存文書
- 用語・命名規則
- 関連仕様・設計書
- テンプレート・文書規約

構成、粒度、用語、表記、記法、図表形式、役割分担を整合させる。

同種文書が複数ある場合は、最新かつ現在も参照されている文書を優先する。

既存文書に問題や矛盾がある場合は、そのまま踏襲しない。矛盾が解消できない場合は、前提または未確定事項として明示する。

### 5. 正確性・一貫性を保つ

以下を徹底する。

- 事実・観測・推測・判断・提案を区別する
- 不明な情報を推測で補完しない
- 必要な根拠・前提・適用範囲を示す
- 用語・表記・命名・記法を統一する
- 数値・条件・単位を明確にする
- 主語・対象・条件・動作を明確にする
- 関連文書との矛盾を確認する
- 一文に複数の論点を詰め込みすぎない
- 略語・専門用語は必要に応じて説明する

情報が不足する場合は推測で補完せず、未確定箇所を`{TODO: 内容}`の形式で明示する。

`TODO`には、確定・確認・対応すべき内容を具体的に記載する。

例：

```text
xxxの上限値は{TODO: xxxの上限値}とする。
```

```text
通信タイムアウトは{TODO: タイムアウト値を決定}とする。
```

```text
本機能の対応モデルは{TODO: 対応モデル一覧を確定}とする。
```

プロジェクトに未確定事項の表記規約がある場合は、それを優先する。

### 6. 保守・利用しやすくする

文書は後から参照・更新・再利用される前提で作成する。

- 同じ情報を重複して記載しない
- 同一情報の正本を一つにする
- 詳細は必要に応じて別文書へ分離し、参照する
- 関連文書との役割分担を明確にする
- 将来の更新で影響範囲を追跡しやすくする
- 再利用しやすい構成にする

## 作成フロー

### 1. 執筆条件を確認する

目的、読者、用途、対象範囲、必須要件、制約、出力形式を確認する。

構成や内容を左右する不明点があればユーザーへ確認する。

回答が得られない場合は、妥当な前提を明示し、確定できない事項は`{TODO: 内容}`として記録する。

### 2. 関連文書を確認する

既存文書、規約、テンプレート、仕様、設計、用語などを確認し、構成・粒度・表記・役割分担を整合させる。

確認対象が取得できない場合は、その事実を明示し、利用可能な情報だけで作成する。確認できていない内容を確認済みとして扱わない。

### 3. 必要な情報と構成を整理する

必要な情報、対象範囲、対象外範囲を整理し、章・節の構成と情報の順序を決める。

### 4. 文書を作成する

整理した構成に従い、必要十分な情報を記載する。

文書種別固有の形式、プロジェクト規約、既存文書のルールを優先する。

### 5. 内容を確認する

「最終確認」のチェックリストを使用して、内容を確認する。

## 判断例

### 例1：情報量

**悪い例**

> 設計書に関連する一般的な技術知識、採用しなかった方式、将来考えられる方式まで詳細に記載する。

**良い例**

> この設計を理解・利用するために必要な前提、設計内容、制約、理由を記載し、詳細は関連資料を参照する。

### 例2：構成

**悪い例**

> 背景 → 詳細仕様 → 結論 → 制約

**良い例**

> 結論 → 必要な前提 → 詳細 → 制約

ただし、背景や定義を先に理解しないと結論を理解できない場合は、背景・定義を先に置く。

### 例3：正確性

**悪い例**

> 最大接続数は128台とする。

根拠がなく、実際の仕様が未確認であるにもかかわらず確定値として記載している。

**良い例**

> 最大接続数は`{TODO: ハードウェア仕様を確認して最大接続数を確定}`とする。

未確認の情報を確定事項として扱わず、後から何を確認すべきか分かるようにする。

### 例4：保守性

**悪い例**

> 各設計書に共通仕様の詳細をそれぞれ記載する。

共通仕様の変更時に複数文書を修正する必要があり、不整合が発生しやすい。

**良い例**

> 共通仕様は正本となる文書に記載し、各設計書から参照する。

情報の正本を明確にし、重複記載を避ける。

## 最終確認

以下をすべて確認する。

- [ ] 目的と対象読者が明確になっている
- [ ] 対象範囲・対象外範囲が適切である
- [ ] 必要な情報だけが含まれている
- [ ] 不要な重複や一般論がない
- [ ] 構成と情報の順序が理解しやすい
- [ ] 結論・重要情報が必要な場合に適切な位置にある
- [ ] 関連文書と用語・表記・内容が整合している
- [ ] 事実と推測・判断・提案が区別されている
- [ ] 不明な情報を推測で補完していない
- [ ] 未確定事項や要対応事項が`{TODO: 内容}`で明示されている
- [ ] TODOの内容から、何を確定・確認・対応すべきか分かる
- [ ] TODOを確定事項と誤認する余地がない
- [ ] 曖昧な主語・対象・条件がない
- [ ] 将来の更新・参照・再利用がしやすい
- [ ] 文書間で同じ情報が不必要に重複していない